import classes from './AddDepButton.module.css'

const AddDepButton = (props) => {
    return (
        <button className={classes.depBtn}>
            Add Dep.
        </button>
    );
};

export default AddDepButton;